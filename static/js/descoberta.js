let currentStep=1;const totalSteps=6;const form=document.getElementById('diagnosisForm');window.addEventListener('DOMContentLoaded',()=>{const savedData=localStorage.getItem('inovamind_diagnosis');if(savedData){try{const data=JSON.parse(savedData);Object.keys(data).forEach(key=>{const inputs=form.querySelectorAll(`[name="${key}"]`);if(inputs.length>0){if(inputs[0].type==='checkbox'){inputs.forEach(input=>{if(data[key].includes(input.value)){input.checked=true;}});}else{inputs[0].value=data[key];}}});}catch(e){console.error('Erro ao restaurar dados',e);}}});function saveState(){const formData=new FormData(form);const data={};for(let[key,value]of formData.entries()){if(data[key]){if(!Array.isArray(data[key])){data[key]=[data[key]];}
data[key].push(value);}else{const inputs=form.querySelectorAll(`[name="${key}"]`);if(inputs.length>1&&inputs[0].type==='checkbox'){data[key]=[value];}else{data[key]=value;}}}
localStorage.setItem('inovamind_diagnosis',JSON.stringify(data));return data;}
function startDiagnosis(){document.getElementById('hero-section').classList.add('hidden');document.getElementById('form-container').classList.remove('hidden');document.getElementById('form-container').scrollIntoView({behavior:'smooth'});}
function updateUI(){document.querySelectorAll('.step').forEach((el,index)=>{if(index+1===currentStep){el.classList.add('active');}else{el.classList.remove('active');}});if(currentStep<=totalSteps){const percent=(currentStep/totalSteps)*100;document.getElementById('progress-bar').style.width=`${percent}%`;document.getElementById('progress-text').innerText=`Etapa ${currentStep} de ${totalSteps}`;}
const btnPrev=document.getElementById('btn-prev');const btnNext=document.getElementById('btn-next');if(currentStep===1){btnPrev.classList.add('invisible');}else{btnPrev.classList.remove('invisible');}
if(currentStep===totalSteps){btnNext.innerText='Enviar Diagnóstico';btnNext.classList.remove('bg-primary');btnNext.classList.add('bg-green-600','hover:bg-green-700','shadow-[0_0_15px_rgba(22,163,74,0.4)]');}else if(currentStep>totalSteps){document.getElementById('nav-buttons').classList.add('hidden');document.getElementById('progress-container').classList.add('hidden');}else{btnNext.innerText='Avançar';btnNext.classList.remove('bg-green-600','hover:bg-green-700','shadow-[0_0_15px_rgba(22,163,74,0.4)]');btnNext.classList.add('bg-primary');}
if(currentStep>1){document.getElementById('form-container').scrollIntoView({behavior:'smooth',block:'start'});}}
function validateCurrentStep(){const stepEl=document.getElementById(`step-${currentStep}`);const requiredInputs=stepEl.querySelectorAll('[required]');let isValid=true;requiredInputs.forEach(input=>{if(!input.value){input.reportValidity();isValid=false;}});return isValid;}
function nextStep(){if(!validateCurrentStep())return;saveState();if(currentStep===totalSteps){submitForm();return;}
if(currentStep<totalSteps){currentStep++;updateUI();}}
function prevStep(){if(currentStep>1){currentStep--;updateUI();}}
function submitForm(){const data=saveState();console.log("=== Diagnóstico Estratégico Submetido ===");console.log(JSON.stringify(data,null,2));localStorage.removeItem('inovamind_diagnosis');currentStep++;updateUI();}
const observerOptions={root:null,rootMargin:'0px',threshold:0.1};const counterObserver=new IntersectionObserver((entries,observer)=>{entries.forEach(entry=>{if(entry.isIntersecting){const counters=entry.target.querySelectorAll('.counter');counters.forEach(counter=>{const target=+counter.getAttribute('data-target');const duration=2000;const stepTime=Math.abs(Math.floor(duration/target));let current=0;const timer=setInterval(()=>{current+=1;counter.innerText=current;if(current>=target){clearInterval(timer);counter.innerText=target;}},stepTime);});observer.unobserve(entry.target);}});},observerOptions);document.addEventListener('DOMContentLoaded',()=>{const countersSection=document.getElementById('counters-section');if(countersSection){counterObserver.observe(countersSection);}});document.addEventListener("click",function(ev){var el=ev.target.closest("[data-open]");if(el){window.open(el.getAttribute("data-open"),"_blank");return;}
var a=ev.target.closest("[data-action]");if(!a)return;var act=a.getAttribute("data-action");if(act==="scroll-hero")document.getElementById("hero-section").scrollIntoView({behavior:"smooth"});else if(act==="next")nextStep();else if(act==="prev")prevStep();else if(act==="start")startDiagnosis();});